
const select = (srcElement, id) => {
    if ("selected" in srcElement.classList) {
        srcElement.classList.remove("selected")
    } else {
        srcElement.classList.add("selected")
    }

    if (id.includes("bed")) {

    } else if (id.includes("wed")) {

    } else if (id.includes("behead")) {

    }
}